turtles-own[x y vx vy]
to setup
  clear-all
  set-default-shape turtles "circle"
  cro n [fd 1 
    set x pxcor
    set y pycor
  ]
end

to go
  ask-concurrent turtles [
    ifelse   abs(pxcor) < 10 and  abs ( pycor ) < 10 
    [fd 1] [set heading -1 * heading fd 1 ]
    
  
  
  ]
end
